============
Installation
============

At the command line::

    $ pip install molteniron.git

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv molteniron.git
    $ pip install molteniron.git
